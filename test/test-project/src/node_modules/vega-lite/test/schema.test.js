"use strict";
var assert = require('assert'), inspect = require('util').inspect;
var schema = require('../lib/schema.json');
var specSchema = require('../vega-lite-schema.json');
var util_1 = require('./util');
describe('Schema', function () {
    it('should be valid', function () {
        var validator = new util_1.zSchema();
        var valid = validator.validate(specSchema, schema);
        if (!valid) {
            var errors = validator.getLastErrors();
            console.log(inspect(errors, { depth: 10, colors: true }));
        }
        assert.equal(valid, true);
    });
});
//# sourceMappingURL=schema.test.js.map