import { Formula } from '../../transform';
import { Dict } from '../../util';
import { FacetModel } from './../facet';
import { LayerModel } from './../layer';
import { Model } from './../model';
import { DataComponent } from './data';
export declare namespace formula {
    const parseUnit: (model: Model) => Dict<Formula>;
    function parseFacet(model: FacetModel): Dict<Formula>;
    function parseLayer(model: LayerModel): Dict<Formula>;
    function assemble(component: DataComponent): any;
}
