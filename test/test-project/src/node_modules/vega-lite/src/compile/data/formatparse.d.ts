import { Dict } from '../../util';
import { FacetModel } from './../facet';
import { LayerModel } from './../layer';
import { Model } from './../model';
export declare namespace formatParse {
    const parseUnit: (model: Model) => Dict<string>;
    function parseFacet(model: FacetModel): Dict<string>;
    function parseLayer(model: LayerModel): Dict<string>;
}
