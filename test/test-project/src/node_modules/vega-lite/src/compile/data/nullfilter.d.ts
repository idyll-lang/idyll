import { Dict } from '../../util';
import { FacetModel } from './../facet';
import { LayerModel } from './../layer';
import { Model } from './../model';
import { DataComponent } from './data';
export declare namespace nullFilter {
    const parseUnit: (model: Model) => Dict<boolean>;
    function parseFacet(model: FacetModel): Dict<boolean>;
    function parseLayer(model: LayerModel): Dict<boolean>;
    function assemble(component: DataComponent): {
        type: string;
        test: string;
    }[];
}
