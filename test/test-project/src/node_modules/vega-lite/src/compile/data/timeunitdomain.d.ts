import { StringSet } from '../../util';
import { VgData } from '../../vega.schema';
import { FacetModel } from './../facet';
import { LayerModel } from './../layer';
import { Model } from './../model';
import { DataComponent } from './data';
export declare namespace timeUnitDomain {
    const parseUnit: (model: Model) => StringSet;
    function parseFacet(model: FacetModel): any;
    function parseLayer(model: LayerModel): any;
    function assemble(component: DataComponent): VgData[];
}
