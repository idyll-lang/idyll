import { Dict } from '../../util';
import { FacetModel } from '../facet';
import { LayerModel } from '../layer';
import { UnitModel } from '../unit';
import { DataComponent } from './data';
export declare namespace colorRank {
    function parseUnit(model: UnitModel): Dict<any[]>;
    function parseFacet(model: FacetModel): Dict<any[]>;
    function parseLayer(model: LayerModel): Dict<any[]>;
    function assemble(component: DataComponent): any;
}
