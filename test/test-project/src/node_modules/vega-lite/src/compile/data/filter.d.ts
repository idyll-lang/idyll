import { FacetModel } from '../facet';
import { LayerModel } from '../layer';
import { Model } from '../model';
import { DataComponent } from './data';
export declare namespace filter {
    function parse(model: Model): string;
    const parseUnit: typeof parse;
    function parseFacet(model: FacetModel): string;
    function parseLayer(model: LayerModel): string;
    function assemble(component: DataComponent): {
        type: string;
        test: string;
    }[];
}
