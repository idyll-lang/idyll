import { Config, Orient, MarkConfig } from '../config';
import { Encoding } from '../encoding';
import { Mark } from '../mark';
import { StackProperties } from '../stack';
export declare function initMarkConfig(mark: Mark, encoding: Encoding, stacked: StackProperties, config: Config): any;
export declare function orient(mark: Mark, encoding: Encoding, markConfig?: MarkConfig): Orient;
