import { Channel } from '../channel';
import { Config } from '../config';
import { FieldDef, OrderChannelDef } from '../fielddef';
import { TimeUnit } from '../timeunit';
import { Model } from './model';
import { UnitModel } from './unit';
import { Spec } from '../spec';
export declare function buildModel(spec: Spec, parent: Model, parentGivenName: string): Model;
export declare const STROKE_CONFIG: string[];
export declare const FILL_CONFIG: string[];
export declare const FILL_STROKE_CONFIG: string[];
export declare function applyColorAndOpacity(p: any, model: UnitModel): void;
export declare function applyConfig(properties: any, config: any, propsList: string[]): any;
export declare function applyMarkConfig(marksProperties: any, model: UnitModel, propsList: string[]): any;
export declare function numberFormat(fieldDef: FieldDef, format: string, config: Config, channel: Channel): string;
export declare function sortField(orderChannelDef: OrderChannelDef): string;
export declare function timeTemplate(templateField: string, timeUnit: TimeUnit, format: string, shortTimeLabels: boolean, config: Config): string;
