#!/usr/bin/env node

var os = require("os"),
    dsv = require("../"),
    read = require("../lib/read");

read(process.argv[2], function(error, text) {
  if (error) throw error;
  process.stdout.write(dsv.tsv.format(dsv.csv.parse(text)));
  process.stdout.write(os.EOL);
});
