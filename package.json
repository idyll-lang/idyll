{
  "name": "idyll",
  "private": true,
  "license": "MIT",
  "scripts": {
    "postinstall": "cd packages/idyll-cli && npm run build && cd ../idyll-components && npm run build && cd ../idyll-document && npm run build"
  },
  "devDependencies": {
    "lerna": "^2.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
