{
  "name": "idyll",
  "private": true,
  "license": "MIT",
  "scripts": {
    "postinstall": "cd packages/idyll-compiler && npm run build-grammar && cd ../idyll-components && npm run build"
  },
  "devDependencies": {
    "lerna": "^2.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
